# 이름짓기
name: Github Actions 연습하기

# on: 언제 이 작업을 실행할지 정의
# push: github에 push 할때마다 실행
on: push

# jobs: 어떤일을 실행 할지 정의
jobs:
  push-job:
    runs-on: ubuntu-latest
    # 일을 어떤 순서로 진행할지 정의
    steps:
      - name: 첫 번째 작업
        run: echo "첫 번째 작업 실행 중"